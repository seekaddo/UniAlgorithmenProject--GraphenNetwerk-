package ad1.ss16.pa;

import java.util.LinkedList;
import java.util.List;

/**
 * Created by addodennis on 5/14/16.
 */
public class Test {
    /*public List<Integer> criticalNodes() {
        List<Integer> critical = new LinkedList<>();
        int numNodes = numberOfNodes();
        boolean[] discovered = new boolean[numNodes];
        int[] time = new int[numNodes];
        int[] low = new int[numNodes];
        int[] parent = new int[numNodes];
        boolean[] isCritical = new boolean[numNodes];

        for (int i = 0; i < numNodes; i++){
            parent[i] = -1;
        }

        for (int u = 0; u < listArrays.size(); u++ ){
            if (discovered[u]) continue;
            criticalHelper(u, discovered, time, low, parent, 0, isCritical);
        }


        for (int i = 0; i < isCritical.length; i++){
            if (isCritical[i]){
                critical.add(i);
            }
        }

        return critical;
    }*/
}
